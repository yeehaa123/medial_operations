%article.syllabus
  = render course
    
  %section.course_program
    %h1 Course Program
    - if sessions.size > 0 && sessions.first.section == nil
      = render sessions.first
    - if sections.size > 0
      = render sections
    - else
      = render sessions